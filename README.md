# Music4Kay

This project was created to store the title/band/artist/composer of all the sheet music that Kay owns so that she can find it quickly instead of having to look at every book she owns.

This is a simple node application using an MySQL database to store the data which runs on a Raspberry Pi.

The code will be written using VSCode as the editor.
